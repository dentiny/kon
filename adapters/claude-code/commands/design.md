---
description: Design-only pipeline — Azusa explores, Mugi plans, then multi-agent debate (Azusa challenges, Mugi revises) before user confirms. No implementation.
argument-hint: <task>
disable-model-invocation: true
---

User invoked `/kon:design` with arguments: $ARGUMENTS

Resolve once:

```bash
export KON_ROOT="$(python3 "$HOME/.kon/lib/_kon_paths.py" root)"
```

Follow `$KON_ROOT/adapters/claude-code/ORCHESTRATION.md` for command `/kon:design`.
Read `$KON_ROOT/commands/design.md` and run the full orchestration flow. Do not answer directly.
