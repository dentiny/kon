---
description: Session retro — propose preferences and conventions from the current session for public or repo memory. Human confirms each save. Orchestrator only.
disable-model-invocation: true
---

User invoked `/kon:retro` with arguments: $ARGUMENTS

Resolve once:

```bash
export KON_ROOT="$(python3 "$HOME/.kon/lib/_kon_paths.py" root)"
```

Follow `$KON_ROOT/adapters/claude-code/ORCHESTRATION.md` for command `/kon:retro`.
Read `$KON_ROOT/commands/retro.md` and run the full orchestration flow. Do not answer directly.
