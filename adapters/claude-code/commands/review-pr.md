---
description: Holistic GitHub PR review — code diff, PR description, existing review comments, and linked issues. Mio only, read-only.
disable-model-invocation: true
---

User invoked `/kon:review-pr` with arguments: $ARGUMENTS

Resolve once:

```bash
export KON_ROOT="$(python3 "$HOME/.kon/lib/_kon_paths.py" root)"
```

Follow `$KON_ROOT/adapters/claude-code/ORCHESTRATION.md` for command `/kon:review-pr`.
Read `$KON_ROOT/commands/review-pr.md` and run the full orchestration flow. Do not answer directly.
