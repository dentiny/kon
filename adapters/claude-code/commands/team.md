---
description: Full kon run — Azusa explores, Mugi writes a plan, Yui implements, Sawako cleans, Mio reviews. No automated testing.
argument-hint: <task>
disable-model-invocation: true
---

User invoked `/kon:team` with arguments: $ARGUMENTS

Resolve once:

```bash
export KON_ROOT="$(python3 "$HOME/.kon/lib/_kon_paths.py" root)"
```

Follow `$KON_ROOT/adapters/claude-code/ORCHESTRATION.md` for command `/kon:team`.
Read `$KON_ROOT/commands/team.md` and run the full orchestration flow. Do not answer directly.
