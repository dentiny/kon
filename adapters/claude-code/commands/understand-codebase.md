---
description: Learn a codebase — Azusa maps it, Jun writes an interactive HTML guide (concepts + architecture) and study pack (flashcards + quiz).
argument-hint: [scope]
disable-model-invocation: true
---

User invoked `/kon:understand-codebase` with arguments: $ARGUMENTS

Resolve once:

```bash
export KON_ROOT="$(python3 "$HOME/.kon/lib/_kon_paths.py" root)"
```

Follow `$KON_ROOT/adapters/claude-code/ORCHESTRATION.md` for command `/kon:understand-codebase`.
Read `$KON_ROOT/commands/understand-codebase.md` and run the full orchestration flow. Do not answer directly.
